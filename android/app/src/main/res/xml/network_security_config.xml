<?xml version="1.0" encoding="utf-8"?>
<!--
  เวทีอวาตาร์เสิร์ฟจาก InAppLocalhostServer ผ่าน http://localhost:8747
  Android 9+ บล็อก cleartext เป็นค่าเริ่มต้น จึงต้องปลดเฉพาะ localhost

  ปลดแค่ localhost เท่านั้น อย่าใส่ android:usesCleartextTraffic="true"
  ใน manifest เพราะนั่นเปิด http ให้ทั้งแอป = เปิดทาง MITM กับทุก endpoint
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <!-- ทุกปลายทางอื่นบังคับ https เท่านั้น -->
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
